# v5 家族一次性 OOS 考试 — 预注册（写于开跑前，此后判据不再移动）

日期：2026-07-25。被试系统 = v5-prep 批次（4448af0 + 考试机制补丁），全默认开关：
sidecar v2、结构否决层（zone/pullback/no-chase/cooldown）、真限价单、决策契约 v2、
PA_ROUTE=auto（claude_cli → 全量 KB）、day-gate = 机械 overlap。

## 样本（选择自由度 = 0）
- `oos/mes_am_v5_sample128.txt`：MES/am，2024-01→2025-12 完整会话池 516 −
  v4 考试已用 128 = 剩余 388，按固定公式等距取 128（round(i·(N−1)/127)），
  覆盖全部 24 个月，与 v4 样本零重叠。
- 经验库：`experience/cases_oos_v5.jsonl` = cases.jsonl 过滤 session ≥ 2026-01-01
  （57→37）。理由：上次 OOS 尸检产生的 20 张卡片 session 日期在 2024–25，
  date-aware 检索会把它们喂给本考试的相邻会话（泄漏）。过滤后对全部考试日期
  检索结果为空库，与 v4 考试条件一致。
- 通道：claude_cli / Opus，PA_DAYFILTER=overlap，window=am，其余默认。

## 判据（预注册）
- **主判据（唯一通过条件）：本样本 PF > 1.0。**
- 副观测（记录用，不作通过判据）：成交中 limit 单占比（行为验证——新契约
  应产出结构位限价回调单）；否决 tag 分布；中位实现 R；成交笔数。
- 预期成交量级：v4.1 同池 20 笔/128 会话；v5 否决层更严，预计 8–20 笔。
  样本小是已知代价（选择「直接考」而非先跑行为切片是用户的明示决定）。

## 纪律
- 一次性：结果出来照实报告，任何旋钮不因结果移动；失败即否定 v5 家族在
  本窗口/本粒度的主张。
- 运行中断（配额/网络）按既有惯例同命令重启，缓存逐字节续跑，不算重考。
- 尸检（review）在考试完成、结果记录之后进行。
